
import optparse as opt
import xml.dom.minidom as xml

if __name__ == '__main__':
    """
    1) Ensure that the script is being run from the Thunder-Streaming directory
    2) Attempt to build the JAR if it doesn't already exist
    3) Examine the JAR to determine which analyses/outputs are available (should the para
    """

